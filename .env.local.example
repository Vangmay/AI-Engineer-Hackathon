# Copy to .env.local and fill in. Never commit real keys.
# Public frontend config
VITE_CONVEX_URL=

# Server-only provider keys. Do not prefix these with VITE_.
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
GEMINI_API_KEY=
LYRIA_API_KEY=

# Convex CLI/deployment config
CONVEX_DEPLOYMENT=
